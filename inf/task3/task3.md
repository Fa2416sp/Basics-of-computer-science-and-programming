# # Task 3
### Декомпозиция задачи

1. **Получение значения верхней границы последовательности N.**
2. **Вывод всех кандидатов в числа Лишрел из отрезка 1, 2, …, N.**

### Шаблон кода программы

```c
#include <stdio.h>
#include <limits.h>

/* Выводит все кандидаты в числа Лишрел из отрезка 1, 2, ..., last_number */
void show_lychrel_candidates(long last_number);

/* Проверяет, является ли number кандидатом в числа Лишрел */
int is_lychrel_candidate(long number);

/* Возвращает обращение числа n */
long reverse(long n);

/* Основная функция */
int main()
{
    /* Получаем значение верхней границы последовательности */
    long last_number;
    printf("Введите верхнюю границу отрезка поиска чисел Лишрел: ");
    scanf("%ld", &last_number);

    /* Выводим все кандидаты в числа Лишрел до last_number */
    show_lychrel_candidates(last_number);

    return 0;
}

void show_lychrel_candidates(long last_number)
{
    long number;

    /* Проверим каждое число в заданном отрезке: */
    for (number = 1; number <= last_number; number++) {
        /* Если оно является кандидатом в числа Лишрел, напечатаем его */
        if (is_lychrel_candidate(number)) {
            printf("%ld\n", number);
        }
    }
}

int is_lychrel_candidate(long number)
{
    long n = number;
    long r = reverse(n);

    /* Повторяем ... */
    do {
        /* Если сумма числа и его обращения переполняет разрядную сетку, */
        if (n > LONG_MAX - r) {
            /* то считаем число искомым кандидатом и завершаем проверку */
            return 1;
        }
        /* иначе вычисляем новое значение, складывая число с обращением */
        n = n + r;

        /* Вычисляем обращение суммы */
        r = reverse(n);

    /* ... пока число не совпадает с обращением */
    } while (n != r);

    /* Считаем, что проверяемое число - не число Лишрел и завершаем проверку */
    return 0;
}

long reverse(long n)
{
    long r = 0;

    do {
        r = r * 10 + n % 10;
        n /= 10;
    } while (n > 0);

    return r;
}
```

### Объяснение кода

#### **1. Основная функция**:
- Запрашивает у пользователя верхнюю границу последовательности.
- Вызывает функцию `show_lychrel_candidates` для вывода всех кандидатов в числа Лишрел до этой границы.
#### **2. Функция `show_lychrel_candidates`**:
- Перебирает все числа от 1 до заданного значения.
- Проверяет каждое число на принадлежность к кандидатам в числа Лишрел с помощью функции `is_lychrel_candidate`.
- Выводит числа, которые являются кандидатами.
#### **3. Функция `is_lychrel_candidate`**:
- Реализует алгоритм “перевернуть и сложить”.
- Проверяет, является ли число кандидатом в числа Лишрел, используя функцию `reverse` для обращения числа.
#### **4. Функция `reverse`**:
- Возвращает число, полученное из исходного числа путем записи его цифр в обратном порядке.

### Дополнительные шаги
#### 1. Создание каталога и файла исходного кода
- Создайте каталог `task3` в каталоге `inf`.
```Shell
mkdir ~/inf
mkdir ~/inf/task3
cd ~/inf/task3
```
- В каталоге `inf/task3` создайте файл исходного кода `lychrel.c` с приведенным выше кодом.
```Shell
vim lychrel.c
```
- Вставьте в файл `lychrel.c` приведенный выше шаблон кода программы.
#### 2. Подготовка файла Makefile
- В каталоге `task3` создайте файл `Makefile` и откройте его в текстовом редакторе:
```Shell
vim Makefile
```
- Вставьте в файл следующий код для сборки программы:
```Makefile
CC = gcc
CFLAGS = -Wall -Wextra -std=c99

all: lychrel

lychrel: lychrel.o
    $(CC) $(CFLAGS) -o lychrel lychrel.o

lychrel.o: lychrel.c
    $(CC) $(CFLAGS) -c lychrel.c

clean:
    rm -f lychrel lychrel.o
```
- Сохраните файл и закройте текстовый редактор.
#### 3 Сборка и запуск программы
- В каталоге `task3` выполните команду для сборки программы:
```Shell
make
```
- После успешной сборки выполните программу:
```Shell
./lychrel
```
- Введите значение верхней границы, например, 2000, и проверьте вывод программы.
```Shell
Введите верхнюю границу отрезка поиска чисел Лишрел: 2000
196
295
394
493
592
689
691
788
790
879
887
978
986
1495
1497
1585
1587
1675
1677
1765
1767
1855
1857
1945
1947
1997
```
#### 4 Отладка программы
- Для отладки программы используйте отладчик GDB. Запустите GDB с вашей программой:
```Shell 
gdb ./lychrel
```
- В GDB установите контрольную точку на строку вызова функции проверки числа:
```Shell
break show_lychrel_candidates
```
- Запустите программу в GDB:
```Shell
run
```
- Введите значение верхней границы, например, 200.
- Когда программа остановится на контрольной точке, подмените значение переменной `number` на 196:
```Shell
set var number = 196
```
- Войдите в функцию `is_lychrel_candidate`:
```Shell
step
```
- Отслеживайте значения переменных `n` и `r`, выполняя функцию по шагам:
```Shell
next
```
- Убедитесь, что 196 действительно является кандидатом в числа Лишрел.
